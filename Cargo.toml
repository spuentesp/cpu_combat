[workspace]
members = [
    "arbiter",
    "competitor",
    "core-challenges",
]

[workspace.dependencies]
# Dependencias que todos los paquetes usarán
tokio = { version = "1", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"